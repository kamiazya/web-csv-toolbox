---
"web-csv-toolbox": patch
---

Restrict `columnCountStrategy` options for object/record-view output to `fill`/`strict`, reject `keep`/`truncate`, and document the purpose-driven strategy matrix (including sparse/header requirements) so array/object behavior stays intuitive.

/**
 * Main thread execution implementations.
 * These are the default implementations that run in the main thread.
 *
 * @internal
 * @module
 */

export { parseStringInMain } from "./parseStringInMain.ts";
export { parseStreamInMain } from "./parseStreamInMain.ts";
export { parseBinaryInMain } from "./parseBinaryInMain.ts";
